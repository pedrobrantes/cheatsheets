# MITMPROXY
Nixpkgs: nixpkgs.mitmproxy
Source: https://github.com/mitmproxy/mitmproxy
Description: Interactive TLS-capable intercepting HTTP proxy for penetration testers and software developers.
Version: 12.2.1

## Commands

### Basics
- mitmproxy: Start interactive TUI.
- mitmdump: Start non-interactive dump mode.

### Essentials
- mitmproxy --mode transparent: Enable transparent proxying.
- mitmproxy -p 8080: Listen on specific port.

### Intermediaries
- mitmproxy --save-stream flows.cap: Log all traffic to a file.
- mitmproxy -s script.py: Run a custom Python script to process flows.

### Advanceds
- mitmproxy --ignore-hosts ^(?!example\.com): Filter traffic only for example.com.

### Uncommons
- mitmproxy --set block_global=false: Allow connections from outside the local network.

## Tips
- Install the CA certificate from http://mitm.it on the target device.

## Disclaimers
- Inspecting HTTPS traffic requires trust in the mitmproxy CA.

## Alternatives
- burpsuite: GUI-based enterprise tool.
- charles: Popular web debugging proxy.

---
# MITMPROXY (PT)
Nixpkgs: nixpkgs.mitmproxy
Source: https://github.com/mitmproxy/mitmproxy
Descrição: Proxy HTTP interativo capaz de interceptar tráfego TLS para testes de penetração e desenvolvimento.
Versão: 12.2.1

## Comandos

### Básicos
- mitmproxy: Inicia a interface interativa (TUI).
- mitmdump: Inicia o modo de despejo (dump) não interativo.

### Essenciais
- mitmproxy --mode transparent: Habilita o modo de proxy transparente.
- mitmproxy -p 8080: Escuta em uma porta específica.

### Intermediários
- mitmproxy --save-stream flows.cap: Registra todo o tráfego em um arquivo.
- mitmproxy -s script.py: Executa um script Python customizado para processar fluxos.

### Avançados
- mitmproxy --ignore-hosts ^(?!example\.com): Filtra o tráfego apenas para example.com.

### Incomuns
- mitmproxy --set block_global=false: Permite conexões de fora da rede local.

## Dicas
- Instale o certificado CA de http://mitm.it no dispositivo alvo.

## Avisos
- Inspecionar tráfego HTTPS requer confiança no certificado CA do mitmproxy.

## Alternativas
- burpsuite: Ferramenta corporativa baseada em interface gráfica.
- charles: Proxy de depuração web popular.

---
This cheatsheet was generated by an AI agent using the skill defined in .agents/skills/cheatsheet-manager.
